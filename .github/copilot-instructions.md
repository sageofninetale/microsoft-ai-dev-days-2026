# Copilot instructions

## Repository snapshot
- This repo is a minimal scaffold for Microsoft AI Dev Days 2026.
- Top-level structure is a split between `backend/` and `frontend/`.
- Each subproject currently only contains a placeholder `README.md`.

## Big-picture architecture
- No application code is present yet; only the directory boundaries (`backend/` vs `frontend/`) are defined.
- There are no documented data flows, service boundaries, or integration contracts beyond that split.

## Developer workflows
- No build, test, or lint configuration files are present (e.g., no `package.json`, `pyproject.toml`, or `Makefile`).
- The only documented entry points are the placeholder READMEs:
  - `README.md`
  - `backend/README.md`
  - `frontend/README.md`

## Conventions and patterns
- There are no repository-specific coding conventions or framework patterns documented yet.
- When adding new code, reflect any new workflows or conventions in the appropriate README so future agents can follow them.

## Integrations
- No external services, APIs, or environment configuration are described in the repo at this time.
