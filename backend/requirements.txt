# Core dependencies for the backend API
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.6.0

# OpenAI / Azure OpenAI
openai==1.12.0

# HTTP client (required by supabase and openai)
httpx==0.28.1

# Database
supabase==2.3.4

# Environment variable management
python-dotenv==1.0.1

# Azure Speech SDK (large binary, only needed for audio transcription)
azure-cognitiveservices-speech==1.35.0
